/**
 * Modo demonstração. Na versão normal ele não existe (DEMO = null).
 * Rodando "npm run demo", o angular.json troca este arquivo por src/demo/demo-backend.ts,
 * que imita o Supabase no próprio navegador com dados de exemplo.
 */
import type { ModoDemo } from './demo-tipo';

export const DEMO: ModoDemo | null = null;
